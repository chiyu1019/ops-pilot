# 负载均衡问题排查

## 会话保持
开启会话保持后流量不均，可检查 cookie 与源 IP 哈希策略。

## 后端摘除不及时
健康检查间隔过长会导致故障节点继续接流量；检查 interval、timeout、unhealthy_threshold。

## 长连接与超时
HTTP/2 与 gRPC 长连接场景下，需要配置 idle timeout 与连接数上限，避免连接堆积。

## 流量不均
确认算法（轮询/最少连接/一致性哈希）、后端权重、以及是否存在慢节点导致连接堆积。
