# 日志规范与检索技巧

## 规范要求
结构化输出（JSON）、统一字段（timestamp、level、service、trace_id）、禁止打印敏感信息、限制单条长度。

## 检索技巧
- 用 trace_id 串联一次请求的全链路日志
- 用 level 与关键词组合缩小范围（level:ERROR AND keyword:timeout）
- 时间范围尽量精确，避免全量扫描
- 高频错误先聚合再分析（按异常类型分组统计）

## 常见问题
日志缺失关键上下文、时间不同步、日志量过大导致检索超时、采样导致关键日志丢失。
